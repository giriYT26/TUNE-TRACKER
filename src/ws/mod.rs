pub mod host;
pub mod team;

// Shared helper types/functions for both handlers can live here, e.g. a
// common function to split a WebSocket into its sender/receiver halves and
// subscribe it to AppState.tx.
